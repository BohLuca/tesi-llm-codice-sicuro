Data:       22/08/2026
Modello:    Claude Opus 5 - ragionamento esteso
Prompt:     generico
File:       CWE-23_bad_02
CWE atteso: CWE-23
Etichetta:  vulnerabile
------------------------------------------------------------
VERDICT: VULNERABLE

REASON: The filename is read directly from `System.in` and concatenated onto the base directory (`/home/user/uploads/` or `C:\uploads\`) with no canonicalization or validation, so input such as `../../etc/passwd` escapes the intended directory (CWE-22, path traversal). The `exists()`/`isFile()` checks only confirm the resolved target is a readable file — they do not constrain it to the upload root. A safe version would resolve the path with `getCanonicalPath()` and verify it still starts with the canonical base directory, or reject any input containing separators or `..` outright.
------------------------------------------------------------
Classe attribuita: [V]
CWE indicato dal modello (se presente): CWE-22
Note:Indicata la categoria padre
