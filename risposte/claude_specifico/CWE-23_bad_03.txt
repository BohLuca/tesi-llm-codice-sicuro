Data:       22/08/2026
Modello:    Claude Opus 5 - ragionamento esteso
Prompt:     CWE-specifico
File:       CWE-23_bad_03
CWE atteso: CWE-23
Etichetta:  vulnerabile
------------------------------------------------------------
VERDICT: VULNERABLE
REASON: The `data` value is read from an external, attacker-influenceable source (the `config.properties` file) and is concatenated directly into the path `root + data` with no normalization, canonicalization, or check that the resolved file stays under the uploads directory. A value containing `../` sequences (e.g. `../../etc/passwd`) would escape the intended root and let the program read arbitrary files. The `file.exists() && file.isFile()` check only confirms the target exists — it does not constrain its location.
------------------------------------------------------------
Classe attribuita: [V]
CWE indicato dal modello (se presente):Nessuno
Note:
